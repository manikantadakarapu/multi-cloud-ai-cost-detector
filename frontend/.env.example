# FastAPI origin, without the /api/v1 suffix.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# Set to true to preview deterministic sample dashboard data without provider credentials.
NEXT_PUBLIC_DEMO_MODE=false
